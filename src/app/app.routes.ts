import { Routes } from '@angular/router';
import { GuitarManagerComponent } from './guitar/guitar-manager.component';

export const routes: Routes = [
  { path: '', component: GuitarManagerComponent }
];